# Environment variables for Advanced Exam Application
# Copy this file to `.env.local` and fill in the values.
# Never commit real secrets to version control.

# --- Supabase (Required) ---
# Public URL of your Supabase project (found in Project Settings -> API)
NEXT_PUBLIC_SUPABASE_URL=https://bkkuqomwttmrpkuvfqwx.supabase.co
# Public anon key (safe for client usage)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJra3Vxb213dHRtcnBrdXZmcXd4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTY2MzE2MTMsImV4cCI6MjA3MjIwNzYxM30.XRSh508BojNCpA83c1Xlh2inOIOaAwb5YDvz-gbLfKU
# Service Role key (server-only, DO NOT expose on the client)
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# --- Authentication Secrets (Required for production) ---
# Secret used to sign/verify admin JWT cookies (HS256). Use a strong random value.
# Required in production. In development, the app falls back to a dev secret if unset.
AUTH_SECRET="set-a-strong-random-secret"
# Optional fallback if you already have NEXTAUTH_SECRET in your environment.
# If AUTH_SECRET is not set, the app will use NEXTAUTH_SECRET if present.
# NEXTAUTH_SECRET="set-if-you-use-nextauth-secret"

# --- App Branding (Optional) ---
# Used as default app title in metadata.
APP_BRAND_NAME="Advanced Exam App"

# --- Script Compatibility (Optional) ---
# Some maintenance scripts also check SUPABASE_URL as a fallback to NEXT_PUBLIC_SUPABASE_URL.
# Keep this value in sync with NEXT_PUBLIC_SUPABASE_URL if you choose to set it.
# SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"

# --- WhatsApp Integration (Optional, not currently used by code) ---
# For future server-side WhatsApp Cloud API integration. Not required today.
# WHATSAPP_TOKEN=""
# WHATSAPP_PHONE_ID=""

# Notes:
# - The code reads env vars at runtime for both server and scripts. Client code only uses NEXT_PUBLIC_* vars.
# - Cookies are set with `Secure` only when NODE_ENV === "production".
# - For deployment, replicate these variables in your hosting provider's env settings.
